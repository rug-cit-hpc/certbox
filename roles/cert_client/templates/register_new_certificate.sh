# This script is responsible to actually make the service running on this machine use the new certificate
# The certificates are found at /etc/pki/{{ cert_client_cert_name }}.pem
# The private key is found at /etc/pki/{{ cert_client_key_name }}.pem

echo "Not implemented yet"
